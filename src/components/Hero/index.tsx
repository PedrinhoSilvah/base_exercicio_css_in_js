import { Formulario, Container, HeroTitle } from './Style'

const Hero = () => (
  <Formulario>
    <Container className="container">
      <HeroTitle>
        As melhores vagas para tecnologia, design e artes visuais.
      </HeroTitle>
    </Container>
  </Formulario>
)

export default Hero
